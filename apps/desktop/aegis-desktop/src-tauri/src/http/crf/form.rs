//! HTTP functions under `/api/crf/versions/{id}/forms` and
//! `/api/crf/forms/{id}`. Real implementation lives in the
//! follow-up Task 3; this file exists to satisfy the `pub mod form`
//! declaration in `crf.rs`.